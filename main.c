#include <stdlib.h>
#include "Calulator Polish Notation .c"
#define forever for(;;)
#undef getchar
#define max(A, B) ((A) > (B) ? (A) : (B))
main()

{
    printf(max(5,3));
    printf("Insert a value");
    getchar();
}